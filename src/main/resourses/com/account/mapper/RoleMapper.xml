<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--接口的全路径作为命名空间  -->
<mapper namespace="com.account.mapper.RoleMapper">
    <select id="queryAll" resultType="com.account.entity.Role">
        select * from t_role
    </select>
</mapper>